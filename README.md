# Blog Generator with Groq + LangChain

## Overview
This project uses Groq’s large language models and LangChain agents to generate well-structured blogs on any topic, leveraging Wikipedia and DuckDuckGo for factual accuracy.

## Installation
```bash
git clone <your_repo_url>
cd blog-generator
python -m venv venv
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows
pip install -r requirements.txt